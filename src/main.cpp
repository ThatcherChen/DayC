#include <iostream>
#include <fstream>
#include <string>

int main(int argc, char *argv[])
{
    using std::string, std::cout, std::cin;
    string model, func, arg;
    cin >> model >> func;
    std::getline(cin, arg);
    // ...
    return 0;
}